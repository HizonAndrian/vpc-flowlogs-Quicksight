# Testing and performing terraform plan
name: Terraform testing
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  testing:
    runs-on: ubuntu-latest

    steps:
      - name: setup testing.
        run: | 
          echo "Set up works."